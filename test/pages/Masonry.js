// @flow
import * as React from 'react';
import Container from '../components/Container';
import renderConfig from '../utils/renderConfig';

export default () => <Container {...renderConfig.Masonry} />;
