// @flow strict-local

type Props = {|
  isFront: boolean,
  x1: string,
  x2: string,
  y1: number,
  y2: number,
  yAxisId: string,
  stroke: string,
  strokeOpacity: number,
|};

/**
 * [ReferenceArea](https://gestalt.pinterest.systems/web/chart) component should be used for ... on the page.
 */
// eslint-disable-next-line no-unused-vars
function ReferenceArea({ x1, x2, y1, y2, yAxisId, stroke, strokeOpacity, isFront }: Props): null {
  return null;
}

export default ReferenceArea;
