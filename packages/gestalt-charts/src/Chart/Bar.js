// @flow strict-local
import { type DataVisualizationColors } from './types.js';

type Props = {|
  yAxis: 'left' | 'right',
  id: string,
  color: DataVisualizationColors,
|};

/**
 * [ReferenceArea](https://gestalt.pinterest.systems/web/chart) component should be used for ... on the page.
 */
// eslint-disable-next-line no-unused-vars
export default function Bar({ yAxis, id, color }: Props): null {
  return null;
}
