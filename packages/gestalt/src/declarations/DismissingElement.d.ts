import type { Node } from 'react';
import 'react';
declare const DismissingElement: ({
  children,
}: {
  children: (arg0: { onDismissStart: () => void }) => Node;
}) => Node;
export default DismissingElement;
