// @flow strict
import { type Node } from 'react';
import VisuallyHidden from './VisuallyHidden.js';

export default function AccessibilityPause(): Node {
  return <VisuallyHidden>,</VisuallyHidden>;
}
