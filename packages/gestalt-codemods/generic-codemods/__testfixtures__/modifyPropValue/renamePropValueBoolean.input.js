import { Box, Button } from 'gestalt';

export default function TestComp() {
  return (
    <Box width={400} fit={false}>
      <Box fit={false}/>
      <Box fit={false}/>
      <Button/>
    </Box>
  );
}
