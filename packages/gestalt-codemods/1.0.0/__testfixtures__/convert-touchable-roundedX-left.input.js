// @flow strict
import { Touchable } from 'gestalt';

export default function RoundedLeft() {
  return (
    <Touchable onTouch={() => {}} shape="roundedLeft">
      roundedLeft
    </Touchable>
  );
}
